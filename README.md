Project Name : Mero Inventory MERN+SQL

The app uses both MongoDB and MySQL for storing data for the application. 

You can start the project by downloading from package.json in both frontend and backends. 